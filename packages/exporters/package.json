{
  "name": "@dm-instamap/exporters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "session-pack": "tsx src/cli/session-pack.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@dm-instamap/core": "workspace:*",
    "jszip": "^3.10.1",
    "sharp": "^0.34.5"
  }
}
