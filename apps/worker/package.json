{
  "name": "@dm-instamap/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "python -m compileall src",
    "dev": "python -m uvicorn dm_instamap_worker.main:app --app-dir src --reload --host 127.0.0.1 --port 8000",
    "lint": "python -m ruff check src tests && python -m ruff format --check src tests && python -m mypy src",
    "test": "python -m unittest discover -s tests"
  }
}
